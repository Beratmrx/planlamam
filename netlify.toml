[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
  # Netlify Dashboard > Site settings > Environment variables'dan VITE_BACKEND_URL ekleyin:
  # Örnek: https://api.sizin-domain.com veya http://VDS_IP:3002
